# BC Better Menu Icons

Replaces the WordPress dashicons font with modern `@wordpress/icons` in the admin.

## Building

Install dependencies, then run `npm run build` to extract SVGs, generate the font, and rebuild the CSS.

## Screenshots

_Add screenshots here later._